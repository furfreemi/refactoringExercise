apply plugin: 'application'
apply plugin: 'idea'
apply plugin: 'jacoco'

repositories {
    jcenter()
}

defaultTasks 'clean', 'test'

dependencies {
    compile 'org.mockito:mockito-all:2.+'
    compile 'org.hamcrest:hamcrest-all:1.+';
    compile 'junit:junit:4+'
}
